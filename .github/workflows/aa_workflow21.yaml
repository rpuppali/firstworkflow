name: Usage of simpleJSaction
on: [push, workflow_dispatch]
jobs:
  getgithubrepo:
    runs-on: ubuntu-latest
    steps:
      - name: displayHelloWorld
        id: greet
        uses: actions/checkout@v3
  
  runShellScript:
    runs-on: ubuntu-latest
    steps:
      - name: run shell
        working-directory: ${{ github.workspace }}/FIRST-WORKFLW
        run: ./shellscript.sh
