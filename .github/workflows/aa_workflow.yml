name: UseOfSimpleAction
on: push

jobs:
  firstJob:
    runs-on: ubuntu-latest
    steps:
      - name: name1
        run: |
           ls -l
           echo "hello"

      - name: name2
        id: greet 
        uses: actions/hello-world-javascript-action@v1.1
        with:
          who-to-greet: "Raghav"
        