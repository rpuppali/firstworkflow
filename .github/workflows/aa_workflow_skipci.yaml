name : first_workflow1
on: [push]

jobs: 
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string 
        run: echo "Hello World"
      - name: skip ci
        run: echo "Skip CI"
  