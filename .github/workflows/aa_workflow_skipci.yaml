name: UsageOfcheckoutAction
on: [push]

jobs:
  UsageOfcheckoutAction:
    runs-on: ubuntu-latest
    steps:
    - name: Display
      run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
# GITHUB_SHA - Last git commit id 
# GITHUB_SHA - Last git commit id 
      
