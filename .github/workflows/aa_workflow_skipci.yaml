name: changeInShell
on: [push]

jobs:
  runonubunntu:
    runs-on: ubuntu-latest
    steps:
      - name: currentDirectory
        run: |
          pwd
          ls -al
  runonwindows:
    runs-on: windows-latest
    steps:
      - name: currentDirectory
        run: |
          pwd
          ls -al
      