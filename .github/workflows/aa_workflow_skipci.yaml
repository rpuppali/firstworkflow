name: changeInDirectory
on: [push]

jobs:
  changeInDirectory:
    runs-on: ubuntu-latest
    steps:
      - name: currentDirectory
        run: |
          pwd
      - name: changeInWorkingDirectory
        working-directory: /home/runner
        run: |
            pwd
            