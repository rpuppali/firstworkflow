name : Running_simple_shell_commands
on: [push]

jobs:
  parallelJobOnMacos:
    runs-on: macos-latest
    steps:
        - name: view sw version
          run: sw_vers
  dependant-job:
    runs-on: windows-latest
    needs: run-shell-commands
    steps:
        - name: echo a string
          run: Write-Output "Windows String"
        - name: echo a string
          run: Write-Output "Skip CI"